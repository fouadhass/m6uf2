#!/bin/bash

read -p "Introdueix la ruta a comprimir: " ruta

# La opicó -e comprovem que existeixi la ruta.
# Un cop comprovat comprimim la ruta.
if [ -e $ruta ]
then

	tar -czvf directori.tar.gz $ruta
 
fi
