#!/bin/bash

read -p "Nombre enter: " num

# La opció -eq comprovem que el nombre sigui igual a 3
if [ $num -eq '3' ]
then

	echo "El nombre introfuit es igual a 3"
 
else

	echo "El nombre es diferent de 3"
 
fi
